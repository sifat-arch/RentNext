enum UserStatus {
  ACTIVE
  BANNED
}

enum Role {
   TENANT
   LANDLORD
   ADMIN 
}

enum BookingStatus {
  PENDING
  APPROVED
  REJECTED
  ACTIVE
  PAID
}

enum PaymentStatus {
  PENDING
  SUCCESS
  FAILED
}

enum PaymentProvider {
   STRIPE
}