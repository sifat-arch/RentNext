enum UserStatus {
  ACTIVE
  BANNED
}

enum Role {
   TENANT
   LANDLORD
   ADMIN 
}

enum BookingStatus {
  PENDING
  APPROVED
  REJECTED
  ACTIVE
  COMPLETED
}

enum PaymentStatus {
  PENDING
  COMPLETED
  FAILED
}

enum PaymentProvider {
   STRIPE
}